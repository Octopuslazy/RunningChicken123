import { createDecorationPattern } from './decoration';
import { createTrapPattern } from './trap';
import { createPlainRoadPattern } from './plainRoad';
import { makeGroundPattern } from './groundOnly';

export { createDecorationPattern };
export { createTrapPattern };
export { createPlainRoadPattern };
export { makeGroundPattern };

export default {
  createDecorationPattern,
  createTrapPattern,
  createPlainRoadPattern,
  makeGroundPattern
};
